import { SearchProductoPipe } from './search-producto.pipe';

describe('SearchProductoPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchProductoPipe();
    expect(pipe).toBeTruthy();
  });
});
